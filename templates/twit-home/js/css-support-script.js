 $(function() {
    if (window.PIE) {        
		$('.signup_btn').each(function() {
            PIE.attach(this);
        });
		$('.text_box').each(function() {
            PIE.attach(this);
        });
		$('.apt_text_box').each(function() {
            PIE.attach(this);
        });	
		$('.city_text_box').each(function() {
            PIE.attach(this);
        });
		$('.state_combo').each(function() {
            PIE.attach(this);
        });
		$('.zipcode_txtbox').each(function() {
            PIE.attach(this);
        });
		$('.business_type_combo').each(function() {
            PIE.attach(this);
        });
		$('.business_type_txtbox').each(function() {
            PIE.attach(this);
        });		
		$('.taxid_txtbox').each(function() {
            PIE.attach(this);
        });
		$('.birth_combo').each(function() {
            PIE.attach(this);
        });
		$('.next_btn').each(function() {
            PIE.attach(this);
        });
		$('.search_box').each(function() {
            PIE.attach(this);
        });
		$('.send_outer').each(function() {
            PIE.attach(this);
        });
		$('.login_btn').each(function() {
            PIE.attach(this);
        });
		
		
 }
});